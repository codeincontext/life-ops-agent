# Copy to .env on the host and set these.

# Host dir mounted at /project — the session's working dir (CLAUDE.md + skills).
PROJECT_PATH=/path/on/host/to/claude-admin

# Timezone.
TZ=Europe/Paris

# Display name of the Remote Control session in the Claude app / web.
SESSION_NAME=life-ops-agent
